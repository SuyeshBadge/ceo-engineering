---
description: Prepare and create a ClickUp task.
agent: builder
---

$ARGUMENTS

This is an external write, now plain `allow` (full automation — no approval prompt exists in this config). Use the `create-clickup` skill for context and review the exact task/list/fields for quality before creating it with `clickup_clickup_create_task` — that review is a best-practice instruction to get the output right, not a permission-level gate. Do not alter other tasks or status.
