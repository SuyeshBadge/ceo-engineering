---
description: Request formatting through the CEO controller.
agent: ceo
---

$ARGUMENTS

Fast path if it's a small, obvious formatting fix; otherwise standard tier. Formatting is a write — route it to `builder` either way.
