---
description: Request non-mutating executable verification through CEO.
agent: ceo
---

$ARGUMENTS

Tier: standard assurance. Delegate to `tester`. Tests must not update snapshots, write coverage/generated artifacts into tracked scope, auto-fix, or install dependencies; any repair routes back to `builder`.
