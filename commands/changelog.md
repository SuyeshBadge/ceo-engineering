---
description: Request a changelog update through the CEO controller.
agent: ceo
---

$ARGUMENTS

Tier: standard (docs write). Route to `builder` to write the changelog entry, then `git add` and `git commit` it — both are plain `allow`, so this runs to completion with no manual hand-off.
