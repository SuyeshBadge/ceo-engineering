---
description: Prepare a release for the user to tag, publish, and deploy.
agent: ceo
---

$ARGUMENTS

Tier: risky/irreversible — out of scope for any agent to execute. Run mandatory `security`; preview version changes, changelog, tag, remotes, publish/deploy targets, and rollback plan in one approval gate. Get explicit user approval, then hand over the exact operational sequence for the user to run. No agent tags, publishes, or deploys.
