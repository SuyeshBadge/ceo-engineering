---
description: Request a read-only session metrics summary through CEO.
agent: ceo
---

$ARGUMENTS

Read-only, fast path. Report cost, token counts, timings, or cache rates only when actually measured; omit unavailable fields and never fabricate examples as results.
