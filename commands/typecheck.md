---
description: Request non-mutating type verification through CEO.
agent: ceo
---

$ARGUMENTS

Tier: standard assurance. Delegate to `tester` with no-emit/non-mutating settings. Block checks that create tracked artifacts or rewrite files; repairs route back to `builder`.
