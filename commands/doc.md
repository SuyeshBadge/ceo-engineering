---
description: Request documentation work through the CEO controller.
agent: ceo
---

$ARGUMENTS

Tier: standard (docs write). Route to `builder`. If the request is fast-path (a small, obvious doc fix), skip straight to `builder` with no further gating.
