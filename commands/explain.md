---
description: Request a bounded codebase explanation through the CEO controller.
agent: ceo
---

$ARGUMENTS

Read-only. Use bounded direct orientation (a couple of known files, one search pass) when that suffices; otherwise delegate the explicit local-evidence shard to `scout`. No writes or executable verification.
